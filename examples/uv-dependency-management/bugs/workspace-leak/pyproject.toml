[project]
name = "workspace-root"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24.0", "my-lib"]

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
my-lib = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
